package testClasses;
public class ParserTest {
	/*
	@Test
	public void testParseMovie() {
		String fileName = "testFile.txt"; 
		//create testFile.txt and place with a movie formatted with the desiredMovie information
		Scanner testScanner = new Scanner(fileName);
		//Movie desiredMovie = new Movie("testTitle", "testMedia", "/I", 1997);
		//Movie parsedMovie = Parser.parseMovie(testScanner.nextLine());
		//assertEquals(desiredMovie, parsedMovie);
		testScanner.close();
	}

	@Test
	public void testParseSeries() {
		String fileName = "testFile.txt"; 
		//create testFile.txt and place with a movie formatted with the desiredMovie information
		Scanner testScanner = new Scanner(fileName);
		//Series desiredSeries = new Series("testTile", "testRange", false);
		Series parsedSeries = Parser.parseSeries(testScanner.nextLine());
		//assertEquals(desiredSeries, parsedSeries);
		testScanner.close();
	}

	@Test
	public void testParseEpisode() {
		String fileName = "testFile.txt"; 
		//create testFile.txt and place with a movie formatted with the desiredMovie information
		Scanner testScanner = new Scanner(fileName);
		//Episode desiredEpisode = new Episode("testTitle", "testSeries", 1997);
	//	Episode parsedEpisode = Parser.parseEpisode(testScanner.nextLine());
		//assertEquals(desiredEpisode, parsedEpisode);
		testScanner.close();
	}
	*/
}
